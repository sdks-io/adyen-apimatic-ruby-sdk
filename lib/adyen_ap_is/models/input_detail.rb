# adyen_ap_is
#
# This file was automatically generated by APIMATIC v3.0 (
# https://www.apimatic.io ).

module AdyenApIs
  # InputDetail Model.
  class InputDetail < BaseModel
    SKIP = Object.new
    private_constant :SKIP

    # Configuration parameters for the required input.
    # @return [Hash[String, String]]
    attr_accessor :configuration

    # Input details can also be provided recursively.
    # @return [Array[SubInputDetail]]
    attr_accessor :details

    # Input details can also be provided recursively (deprecated).
    # @return [Array[SubInputDetail]]
    attr_accessor :input_details

    # In case of a select, the URL from which to query the items.
    # @return [String]
    attr_accessor :item_search_url

    # In case of a select, the items to choose from.
    # @return [Array[Item]]
    attr_accessor :items

    # The value to provide in the result.
    # @return [String]
    attr_accessor :key

    # True if this input value is optional.
    # @return [TrueClass | FalseClass]
    attr_accessor :optional

    # The type of the required input.
    # @return [String]
    attr_accessor :type

    # The value can be pre-filled, if available.
    # @return [String]
    attr_accessor :value

    # A mapping from model property names to API property names.
    def self.names
      @_hash = {} if @_hash.nil?
      @_hash['configuration'] = 'configuration'
      @_hash['details'] = 'details'
      @_hash['input_details'] = 'inputDetails'
      @_hash['item_search_url'] = 'itemSearchUrl'
      @_hash['items'] = 'items'
      @_hash['key'] = 'key'
      @_hash['optional'] = 'optional'
      @_hash['type'] = 'type'
      @_hash['value'] = 'value'
      @_hash
    end

    # An array for optional fields
    def self.optionals
      %w[
        configuration
        details
        input_details
        item_search_url
        items
        key
        optional
        type
        value
      ]
    end

    # An array for nullable fields
    def self.nullables
      []
    end

    def initialize(configuration: SKIP, details: SKIP, input_details: SKIP,
                   item_search_url: SKIP, items: SKIP, key: SKIP,
                   optional: SKIP, type: SKIP, value: SKIP)
      @configuration = configuration unless configuration == SKIP
      @details = details unless details == SKIP
      @input_details = input_details unless input_details == SKIP
      @item_search_url = item_search_url unless item_search_url == SKIP
      @items = items unless items == SKIP
      @key = key unless key == SKIP
      @optional = optional unless optional == SKIP
      @type = type unless type == SKIP
      @value = value unless value == SKIP
    end

    # Creates an instance of the object from a hash.
    def self.from_hash(hash)
      return nil unless hash

      # Extract variables from the hash.
      configuration = hash.key?('configuration') ? hash['configuration'] : SKIP
      # Parameter is an array, so we need to iterate through it
      details = nil
      unless hash['details'].nil?
        details = []
        hash['details'].each do |structure|
          details << (SubInputDetail.from_hash(structure) if structure)
        end
      end

      details = SKIP unless hash.key?('details')
      # Parameter is an array, so we need to iterate through it
      input_details = nil
      unless hash['inputDetails'].nil?
        input_details = []
        hash['inputDetails'].each do |structure|
          input_details << (SubInputDetail.from_hash(structure) if structure)
        end
      end

      input_details = SKIP unless hash.key?('inputDetails')
      item_search_url =
        hash.key?('itemSearchUrl') ? hash['itemSearchUrl'] : SKIP
      # Parameter is an array, so we need to iterate through it
      items = nil
      unless hash['items'].nil?
        items = []
        hash['items'].each do |structure|
          items << (Item.from_hash(structure) if structure)
        end
      end

      items = SKIP unless hash.key?('items')
      key = hash.key?('key') ? hash['key'] : SKIP
      optional = hash.key?('optional') ? hash['optional'] : SKIP
      type = hash.key?('type') ? hash['type'] : SKIP
      value = hash.key?('value') ? hash['value'] : SKIP

      # Create object from extracted values.
      InputDetail.new(configuration: configuration,
                      details: details,
                      input_details: input_details,
                      item_search_url: item_search_url,
                      items: items,
                      key: key,
                      optional: optional,
                      type: type,
                      value: value)
    end

    # Provides a human-readable string representation of the object.
    def to_s
      class_name = self.class.name.split('::').last
      "<#{class_name} configuration: #{@configuration}, details: #{@details}, input_details:"\
      " #{@input_details}, item_search_url: #{@item_search_url}, items: #{@items}, key: #{@key},"\
      " optional: #{@optional}, type: #{@type}, value: #{@value}>"
    end

    # Provides a debugging-friendly string with detailed object information.
    def inspect
      class_name = self.class.name.split('::').last
      "<#{class_name} configuration: #{@configuration.inspect}, details: #{@details.inspect},"\
      " input_details: #{@input_details.inspect}, item_search_url: #{@item_search_url.inspect},"\
      " items: #{@items.inspect}, key: #{@key.inspect}, optional: #{@optional.inspect}, type:"\
      " #{@type.inspect}, value: #{@value.inspect}>"
    end
  end
end
