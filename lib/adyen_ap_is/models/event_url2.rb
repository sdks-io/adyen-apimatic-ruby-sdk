# adyen_ap_is
#
# This file was automatically generated by APIMATIC v3.0 (
# https://www.apimatic.io ).

module AdyenApIs
  # The list of local and public URLs to send event notifications to when using
  # Terminal API.
  class EventUrl2 < BaseModel
    SKIP = Object.new
    private_constant :SKIP

    # One or more local URLs to send event notifications to when using Terminal
    # API.
    # @return [Array[Url]]
    attr_accessor :event_local_urls

    # One or more public URLs to send event notifications to when using Terminal
    # API.
    # @return [Array[Url]]
    attr_accessor :event_public_urls

    # A mapping from model property names to API property names.
    def self.names
      @_hash = {} if @_hash.nil?
      @_hash['event_local_urls'] = 'eventLocalUrls'
      @_hash['event_public_urls'] = 'eventPublicUrls'
      @_hash
    end

    # An array for optional fields
    def self.optionals
      %w[
        event_local_urls
        event_public_urls
      ]
    end

    # An array for nullable fields
    def self.nullables
      []
    end

    def initialize(event_local_urls: SKIP, event_public_urls: SKIP)
      @event_local_urls = event_local_urls unless event_local_urls == SKIP
      @event_public_urls = event_public_urls unless event_public_urls == SKIP
    end

    # Creates an instance of the object from a hash.
    def self.from_hash(hash)
      return nil unless hash

      # Extract variables from the hash.
      # Parameter is an array, so we need to iterate through it
      event_local_urls = nil
      unless hash['eventLocalUrls'].nil?
        event_local_urls = []
        hash['eventLocalUrls'].each do |structure|
          event_local_urls << (Url.from_hash(structure) if structure)
        end
      end

      event_local_urls = SKIP unless hash.key?('eventLocalUrls')
      # Parameter is an array, so we need to iterate through it
      event_public_urls = nil
      unless hash['eventPublicUrls'].nil?
        event_public_urls = []
        hash['eventPublicUrls'].each do |structure|
          event_public_urls << (Url.from_hash(structure) if structure)
        end
      end

      event_public_urls = SKIP unless hash.key?('eventPublicUrls')

      # Create object from extracted values.
      EventUrl2.new(event_local_urls: event_local_urls,
                    event_public_urls: event_public_urls)
    end

    # Provides a human-readable string representation of the object.
    def to_s
      class_name = self.class.name.split('::').last
      "<#{class_name} event_local_urls: #{@event_local_urls}, event_public_urls:"\
      " #{@event_public_urls}>"
    end

    # Provides a debugging-friendly string with detailed object information.
    def inspect
      class_name = self.class.name.split('::').last
      "<#{class_name} event_local_urls: #{@event_local_urls.inspect}, event_public_urls:"\
      " #{@event_public_urls.inspect}>"
    end
  end
end
