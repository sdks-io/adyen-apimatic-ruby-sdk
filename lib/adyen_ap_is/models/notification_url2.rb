# adyen_ap_is
#
# This file was automatically generated by APIMATIC v3.0 (
# https://www.apimatic.io ).

module AdyenApIs
  # The list of local and public URLs to send display notifications to when
  # using Terminal API.
  class NotificationUrl2 < BaseModel
    SKIP = Object.new
    private_constant :SKIP

    # One or more local URLs to send notifications to when using Terminal API.
    # @return [Array[Url]]
    attr_accessor :local_urls

    # One or more public URLs to send notifications to when using Terminal API.
    # @return [Array[Url]]
    attr_accessor :public_urls

    # A mapping from model property names to API property names.
    def self.names
      @_hash = {} if @_hash.nil?
      @_hash['local_urls'] = 'localUrls'
      @_hash['public_urls'] = 'publicUrls'
      @_hash
    end

    # An array for optional fields
    def self.optionals
      %w[
        local_urls
        public_urls
      ]
    end

    # An array for nullable fields
    def self.nullables
      []
    end

    def initialize(local_urls: SKIP, public_urls: SKIP)
      @local_urls = local_urls unless local_urls == SKIP
      @public_urls = public_urls unless public_urls == SKIP
    end

    # Creates an instance of the object from a hash.
    def self.from_hash(hash)
      return nil unless hash

      # Extract variables from the hash.
      # Parameter is an array, so we need to iterate through it
      local_urls = nil
      unless hash['localUrls'].nil?
        local_urls = []
        hash['localUrls'].each do |structure|
          local_urls << (Url.from_hash(structure) if structure)
        end
      end

      local_urls = SKIP unless hash.key?('localUrls')
      # Parameter is an array, so we need to iterate through it
      public_urls = nil
      unless hash['publicUrls'].nil?
        public_urls = []
        hash['publicUrls'].each do |structure|
          public_urls << (Url.from_hash(structure) if structure)
        end
      end

      public_urls = SKIP unless hash.key?('publicUrls')

      # Create object from extracted values.
      NotificationUrl2.new(local_urls: local_urls,
                           public_urls: public_urls)
    end

    # Provides a human-readable string representation of the object.
    def to_s
      class_name = self.class.name.split('::').last
      "<#{class_name} local_urls: #{@local_urls}, public_urls: #{@public_urls}>"
    end

    # Provides a debugging-friendly string with detailed object information.
    def inspect
      class_name = self.class.name.split('::').last
      "<#{class_name} local_urls: #{@local_urls.inspect}, public_urls: #{@public_urls.inspect}>"
    end
  end
end
